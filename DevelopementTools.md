# Development tools

## Debuging
```bash
# var dump a variable
dump($someVar);

# var dump a variable with procsess died
dd($someVar);
```

## Profiler
A tool to view a 
```bash
composer require symfony/profiler-pack --dev
```