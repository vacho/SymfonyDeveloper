    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}
        {{ form_row(form.size, {'label': 'Size [Number]'}) }}
        {{ form_row(form.save) }}
    
    {{ form_end(form)}}
